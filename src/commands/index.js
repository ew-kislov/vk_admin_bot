const handleInitCommand = require('./initCommand')
const handleSendCommand = require('./sendCommand')
const handleBroadcastCommand = require('./broadcastCommand')
const handleKickCommand = require('./kickCommand')
const handleGrantCommand = require('./grantCommand')

module.exports = {
    handleInitCommand,
    handleSendCommand,
    handleBroadcastCommand,
    handleKickCommand,
    handleGrantCommand
}